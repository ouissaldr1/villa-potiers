<form method="post" action="{{ path('app_description_delete', {'id': description.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ description.id) }}">
    <button class="btn">Delete</button>
</form>
